let carros = []

let mes = prompt("Mês de vendas:")
let qtd = prompt("Quantidade de vendas:")

if (mes === "janeiro") {
  for (let num = 1; num <= qtd; num++) {
    var carro = prompt(`Carro ${num}:`)
    carros.push(carro)
  }
  console.log(carros)
  console.log(`Quantidade de carros vendidos em ${mes}: ${qtd}`)
}
if (mes === "fevereiro") {
  for (let num = 1; num <= qtd; num++) {
    var carro = prompt(`Carro ${num}:`)
    carros.push(carro)
  }
  console.log(carros)
  console.log(`Quantidade de carros vendidos em ${mes}: ${qtd}`)
}
if (mes === "março") {
  for (let num = 1; num <= qtd; num++) {
    var carro = prompt(`Carro ${num}:`)
    carros.push(carro)
  }
  console.log(carros)
  console.log(`Quantidade de carros vendidos em ${mes}: ${qtd}`)
}
if (mes === "abril") {
  for (let num = 1; num <= qtd; num++) {
    var carro = prompt(`Carro ${num}:`)
    carros.push(carro)
  }
  console.log(carros)
  console.log(`Quantidade de carros vendidos em ${mes}: ${qtd}`)
}
if (mes === "maio") {
  for (let num = 1; num <= qtd; num++) {
    var carro = prompt(`Carro ${num}:`)
    carros.push(carro)
  }
  console.log(carros)
  console.log(`Quantidade de carros vendidos em ${mes}: ${qtd}`)
}
if (mes === "junho") {
  for (let num = 1; num <= qtd; num++) {
    var carro = prompt(`Carro ${num}:`)
    carros.push(carro)
  }
  console.log(carros)
  console.log(`Quantidade de carros vendidos em ${mes}: ${qtd}`)
}
if (mes === "julho") {
  for (let num = 1; num <= qtd; num++) {
    var carro = prompt(`Carro ${num}:`)
    carros.push(carro)
  }
  console.log(carros)
  console.log(`Quantidade de carros vendidos em ${mes}: ${qtd}`)
}
if (mes === "agosto") {
  for (let num = 1; num <= qtd; num++) {
    var carro = prompt(`Carro ${num}:`)
    carros.push(carro)
  }
  console.log(carros)
  console.log(`Quantidade de carros vendidos em ${mes}: ${qtd}`)
}
if (mes === "setembro") {
  for (let num = 1; num <= qtd; num++) {
    var carro = prompt(`Carro ${num}:`)
    carros.push(carro)
  }
  console.log(carros)
  console.log(`Quantidade de carros vendidos em ${mes}: ${qtd}`)
}
if (mes === "outubro") {
  for (let num = 1; num <= qtd; num++) {
    var carro = prompt(`Carro ${num}:`)
    carros.push(carro)
  }
  console.log(carros)
  console.log(`Quantidade de carros vendidos em ${mes}: ${qtd}`)
}
if (mes === "novembro") {
  for (let num = 1; num <= qtd; num++) {
    var carro = prompt(`Carro ${num}:`)
    carros.push(carro)
  }
  console.log(carros)
  console.log(`Quantidade de carros vendidos em ${mes}: ${qtd}`)
}
if (mes === "dezembro") {
  for (let num = 1; num <= qtd; num++) {
    var carro = prompt(`Carro ${num}:`)
    carros.push(carro)
  }
  console.log(carros)
  console.log(`Quantidade de carros vendidos em ${mes}: ${qtd}`)
}